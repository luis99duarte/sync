import java.util.Scanner;

public class daa006 {
    FastScanner in = new FastScanner(System.in);

    int n = in.nextInt; // número de casos

    for(int i = 0; i < n; i++ ) {
        int qx = in.nextInt(); // qx e qx representa o canto inferior esquerdo de cada quadrado
        int qy = in.nextInt();
        int q1 = in.nextInt(); // q1 representa o tamanho de cada lado do quadrado

        int cx = in.nextInt(); // cx e cy representam o centro do círculo
        int cy = in.nextInt();
        int cr = in.nextInt(); // cr representa o raio do círculo
        
    }
}